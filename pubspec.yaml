name: doctorapp
description: app for medical care

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  location: ^3.0.0
  geolocator: ^3.0.1
  firebase_auth: ^0.15.5+3
  modal_progress_hud: ^0.1.3
  cloud_firestore: ^0.13.5
  google_maps_flutter: ^0.5.33
  google_map_polyline: ^0.2.0+1
  flutter_polyline_points: ^0.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter



flutter:

  uses-material-design: true

  assets:
  - images/EmergencyButton.jpeg
  - images/Emergency1.png
